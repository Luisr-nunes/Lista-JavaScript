function calcularRaizQuadrada(a){
    return Math.sqrt(a);
}

let resultado = calcularRaizQuadrada(9)
console.log(`O resultado da raiz quadrada é: ${resultado}`)
module.exports = { calcularRaizQuadrada }